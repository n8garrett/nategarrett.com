---
title: Blog
description: Blogs posts on web and digital marketing
change_freq: monthly
container_size: large
---
{% extends "layout.njk" %}

{% block content %}

<div class="section">
    <div class="container container--large">

        <h1>{{ title }}</h1>
        <ul>
            {%- for post in collections.post -%}
                <li><a href="{{ post.url }}">{{ post.data.title }}</a><br /><span class="date">{{ post.data.date.toDateString() }}</span></li>
            {%- endfor -%}
        </ul>
    </div>
</div>

{% endblock %}